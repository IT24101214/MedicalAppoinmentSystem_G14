<?xml version="1.0" encoding="UTF-8"?>
<web-app xmlns="https://jakarta.ee/xml/ns/jakartaee"
         xmlns:xsi="http://www.w3.org/2001/XMLSchema-instance"
         xsi:schemaLocation="https://jakarta.ee/xml/ns/jakartaee https://jakarta.ee/xml/ns/jakartaee/web-app_5_0.xsd"
         version="5.0">

    <display-name>Medical Appointment Management System</display-name>

    <!-- Listeners -->
    <listener>
        <listener-class>com.medicalsystem.Admin.utils.AppContextListener</listener-class>
    </listener>

    <!-- Welcome File List -->
    <welcome-file-list>
        <welcome-file>index.jsp</welcome-file>
    </welcome-file-list>

    <!-- Session Configuration -->
    <session-config>
        <session-timeout>30</session-timeout>
    </session-config>

    <!-- Security Constraint -->
    <security-constraint>
        <web-resource-collection>
            <web-resource-name>Admin Area</web-resource-name>
            <url-pattern>/adminDashboard.jsp</url-pattern>
            <url-pattern>/manageAdmins.jsp</url-pattern>
            <url-pattern>/registerAdmin.jsp</url-pattern>
            <url-pattern>/updateAdmin.jsp</url-pattern>
        </web-resource-collection>
        <auth-constraint>
            <role-name>admin</role-name>
            <role-name>superadmin</role-name>
        </auth-constraint>
    </security-constraint>
</web-app>